# python库

## pandas 

文件如下

                                        text                  box
    000000000009.jpg  'remove dining table.'    (20, 0, 615, 480)
    000000000025.jpg    'strike out person.'   (187, 88, 77, 131)
    000000000030.jpg        'delete person.'    (2, 88, 287, 318)
    000000000034.jpg          'delete dirt.'  (169, 71, 425, 207)
    000000000036.jpg        'remove flower.'  (13, 341, 213, 179)

存储

    # text 和 box 是列索引，text_list是列表
    data={'text':text_list,'box':box_list}
    # index是行索引
    df=pd.DataFrame(data, index=index_list)

    # 存储成csv格式，sep表示分隔符是逗号
    # header=True 表示第一行是列索引
    df.to_csv('text.csv',sep=',',header=True)

读取

    # header=0表示第0行是列索引
    # index_col=0表示第0列是行索引
    data=pd.read_csv(df_path,header=0,index_col=0)

索引 

    # loc 使用索引
    text=data.loc['000000000009.jpg']['text']

    # iloc 使用行、列号索引
    text=data.iloc[0][0]
